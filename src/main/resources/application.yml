server:
  port: 8082
  shutdown: graceful # 关闭应用时，等待所有请求完成再关闭。默认是immediate（立即关闭）


spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen:7b
        temperature: 0.7  # 创造力

